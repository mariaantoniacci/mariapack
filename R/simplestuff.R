# function to build the function is function

# use graph parenthesis to open and close the function 
# using x and y gives back the element z
function(x,y) {
  z=x+y
return(z)
  }

# i'll assing this to an object
simplestuff <- function(x,y) {
  z=x+y
return(z)
  }
